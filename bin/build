#!/usr/bin/env bash

set -e
set -o pipefail

echo "ℹ️ Downloading dependencies..."
clojure -Srepro --report stderr -P

echo "ℹ️ Updating pom.xml..."
clojure -Srepro --report stderr -Spom

echo "ℹ️ Building uberjar..."
clojure -Srepro --report stderr -X:uberjar
